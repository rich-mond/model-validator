# Contributing

Build and test before submitting changes:

```text
dotnet build ModelValidator.slnx -c Release
dotnet test ModelValidator.slnx -c Release --no-build
```

Do not commit challenge source repositories, hidden validators, secrets, credentials or generated run workspaces to this public framework repository.
