# Scoring

Results are objective and unweighted.

Each assertion is `passed`, `failed`, `timed-out` or `infrastructure-error`. Requirement and regression coverage are reported separately. A task is resolved only when every required requirement and regression assertion passes.

No human judgement, LLM judge, source-similarity score or oracle-patch comparison contributes to computed results.
